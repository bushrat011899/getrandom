[package]
name = "getrandom-backend-windows"
version = "0.4.0"
edition = "2021"
rust-version = "1.78" # Sync tests.yml and README.md.
authors = ["The Rand Project Developers"]
license = "MIT OR Apache-2.0"
description = "A getrandom backend suitable for Windows 10 and later"
documentation = "https://docs.rs/getrandom"
repository = "https://github.com/rust-random/getrandom"
categories = ["os", "no-std"]
exclude = [".*"]

[dependencies]
getrandom-core = { path = "../getrandom-core" }
